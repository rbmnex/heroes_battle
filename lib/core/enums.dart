enum CardType {
  attack,
  buff,
  reaction,
  item,
}

enum AttackCategory {
  melee,
  range,
  magic,
}

enum AttackType {
  normal,
  heavy,
  swift,
}

enum TargetType {
  single,
  multi,
  area,
}

enum ItemType {
  oneTime,
  equipment,
}
